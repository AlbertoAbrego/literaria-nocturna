# Base URL of the Literaria Nocturna backend API
# In development, /api is proxied to http://localhost:3000/api via Vite
# In production, set the full URL (e.g., https://api.example.com/api)
VITE_API_URL=/api
